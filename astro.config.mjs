import { defineConfig } from 'astro/config';

// Update these two values to match your GitHub username and repo name
// e.g. site: 'https://jnguyen0304.github.io', base: '/secarch-hub'
export default defineConfig({
  output: 'static',
  site: 'https://L8terz.github.io',
  base: '/cyber_repo',
});
